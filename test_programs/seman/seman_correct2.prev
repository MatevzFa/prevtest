{
    none : f(1,2,3,i) + 5

    where

        fun f(a: a, b: b, c: c, d: d): int = {

            a = c + b + d
            
            : (a + i + f() + g())

            where
                #typ a: int; # to more crashat z location v 7.13
                var i: int;
                fun f(): int = {
                    none : 6

                    where
                        fun f(): int
                }
        };

        typ a: int;
        typ b: a;
        typ c: b;
        typ d: c;

        var i: int;
        typ integer: int;

        fun g(): int = 1;


        # ???
        # does not fail: function not used

        fun declaration(): bool 

        # ???
}
